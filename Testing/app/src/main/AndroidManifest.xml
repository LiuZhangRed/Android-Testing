<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jerry.testing">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/info"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.WelComeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.MoreActivity" />
        <activity android:name=".activity.InfoActivity" />
        <activity android:name=".activity.AddHabitActivity" />
        <activity android:name=".activity.PersonInformationActivity" />
        <activity android:name=".activity.UpdataNicknameActivity" />
        <activity android:name=".activity.UpdataEmailActivity"></activity>
    </application>

</manifest>